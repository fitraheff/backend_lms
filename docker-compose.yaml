version: "3.9"

services:
  # api:
  #   container_name: lms-api
  #   build: .
  #   command: sh -c "npx prisma migrate deploy && npx prisma generate && npm run dev"
  #   ports:
  #     - "8000:8000"
  #   env_file:
  #     - .env
  #   depends_on:
  #     - postgres
  #     - redis
  #   volumes:
  #     # - .:/app
  #     - /app/node_modules
  #   #Docker akan otomatis me-restart kontainer
  #   restart: unless-stopped
  #   # ========== INI YANG DIAKTIFKAN ==========
  #   develop:
  #     watch:
  #       - action: sync
  #         path: .
  #         target: /app
  #         # initial_sync: true
  #         ignore:
  #           - node_modules/
  #           - .git/
  #           - "*.log"
  #       - action: rebuild
  #         path: package.json
  #       # # Action 1: Sync file biasa (untuk .js, .json, dll)
  #       # - path: ./src
  #       #   action: sync
  #       #   target: /app/src
  #       #   ignore:
  #       #     - node_modules/
  #       #     - .git/
  #       #     - dist/
  #       #     - "*.log"
        
  #       # # Action 2: Sync + Restart untuk file prisma (karena generate ulang)
  #       # - path: ./prisma
  #       #   action: sync+restart
  #       #   target: /app/prisma
  #       #   ignore:
  #       #     - node_modules/
  #       #     - "*.db"
        
  #       # # Action 3: Rebuild kalau package.json berubah
  #       # - path: package.json
  #       #   action: rebuild
        
  #       # # Action 4: Sync untuk file konfigurasi
  #       # - path: .env
  #       #   action: sync+restart
  #       #   target: /app/.env
  #   # ==========================================

  postgres:
    container_name: lms-postgres
    image: postgres:16-alpine
    ports:
      - "5435:5432"
    environment: 
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    container_name: lms-redis
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/var/lib/redis/data
    restart: unless-stopped

  # rabbitmq:
  #   image: rabbitmq:management-alpine
  #   environment:
  #     # Pakai user non-guest (guest dibatasi ke localhost oleh RabbitMQ)
  #     RABBITMQ_DEFAULT_USER: appuser
  #     RABBITMQ_DEFAULT_PASS: appsecret
  #   ports:
  #     - "5672:5672"    # AMQP
  #     - "15672:15672"  # Management UI
  #   healthcheck:
  #     test: ["CMD", "rabbitmq-diagnostics", "ping"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 20

volumes:
  postgres_data:
  redis_data: